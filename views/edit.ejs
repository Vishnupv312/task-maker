<!DOCTYPE html>

<Head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</Head>

<body>
    <div class=" main w-full min-h-screen bg-zinc-900 p-10  ">
        <div class=" text-center text-white font-mono font-semibold pb-6 text-xl">Task Maker</div>
        <a class="text-white bg-blue-600 border-white py-2 px-4" href="/">Go Back To Home</a>

        <form action="/update/<%= filename %>" method="post" class="mt-12">
            <div class="flex w-full flex-row gap-4">
                <div class="flex-col gap-2 w-1/2">
                    <h2 class="text-white text-center tracking-wider mb-8 font-mono text-3xl">Filename</h2>
                    <input class="w-full bg-zinc-700 text-white border-none rounded-md py-2 px-4 mb-4" name="previous"
                        placeholder="Previous Name" value="  <%= filename %>">
                    </input>
                    <input class="w-full bg-zinc-700 text-white border-none rounded-md py-2 px-4 mb-4" name="newName"
                        placeholder=" New Name "> </input>
                </div>
                <div class="flex-col gap-2 w-1/2">
                    <h2 class="text-white text-center tracking-wider mb-8 font-mono text-3xl">Filedata</h2>
                    <input class="w-full bg-zinc-700 text-white border-none rounded-md py-2 px-4 mb-4"
                        name="previousContent" placeholder="Previous File Data" value="<%= fileData %>" disabled>
                    </input>
                    <input class="w-full bg-zinc-700 text-white border-none rounded-md py-2 px-4 mb-4" name="newContent"
                        placeholder=" New File Data"></input>
                </div>
            </div>
            <Button type="submit" class="px-4 py-2 bg-blue-600 hover:bg-blue-500 rounded-md text-white mb-6"> Update the
                Task
                Task</Button>
        </form>
    </div>

</body>